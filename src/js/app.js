const denticon = require('./denticon');

const options = {
    pixels: 'few',
    pixelShape: 'square',
    strokeWidth: 1,
    size: 50,
    containerShape: 'circle',
    borderWidth: 3,
};

denticon.denticon('#denticon', 'emma hewitt', options);
