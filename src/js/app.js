const denticon = require('./denticon');

const options = {
    pixels: 'many',
    shape: 'square',
    strokeWidth: 1,
    strokeColor: '#353535',
};

denticon.denticon('#denticon', 'emma hewitt', options);
